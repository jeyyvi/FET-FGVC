parent: ./configs/cub/base.yaml

dataset:
  root_dir: ~/workspace/dataset/CUB_200_2011/

backbone:
  pretrain_root: "/root/workspace/pretrained/vit/"

train:
  stage: train # ← REQUIRED
  num_workers: 2 # (Colab supports max 2 workers)

val:
  stage: val # ← REQUIRED
  num_workers: 2
